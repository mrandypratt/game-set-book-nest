/**
 * CourtConfiguration is an enum that describes how the court is configured
 * with respect to Tennis and Pickleball.
 */
export enum CourtConfiguration {
  TennisOnly = 1,
  PickleballOnly = 2,
  TennisAndPickleball = 3,
  TennisAndTwoPickleball = 4,
}
